<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:listDivider">@color/bright_foreground_disabled_material_dark</item>
        <!-- Customize your theme here. -->
    </style>

</resources>
